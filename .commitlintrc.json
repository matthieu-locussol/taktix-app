{
   "extends": ["@commitlint/config-conventional"],
   "rules": {
      "type-enum": [
         2,
         "always",
         ["fix", "build", "chore", "ci", "docs", "feat", "refactor", "style", "test"]
      ]
   }
}
